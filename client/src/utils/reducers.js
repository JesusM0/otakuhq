import {} from './actions';

const initialState = {
  products: [],
  categories: [],
  currentCategory: '',
  cart: [],
  cartOpen: false,
};

export const reducer = (state = initialState, action) => {
  //   default:
  //     return state;
  // }
};
